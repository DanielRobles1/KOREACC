// Este archivo es reemplazado por environment.prod.ts en cualquier build de producción
// (fileReplacements en angular.json). HTTP es intencional para desarrollo local únicamente.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
